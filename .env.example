# Postgres Credentials fuer docker-compose
# In .env kopieren: cp .env.example .env  und Werte anpassen
POSTGRES_USER=clusterlogai
POSTGRES_PASSWORD=change_me_to_something_strong
POSTGRES_DB=clusterlogai
POSTGRES_PORT=5432

# Zeitzone des analyzer-Containers (fuer Log-Zeitstempel)
TZ=Europe/Berlin
